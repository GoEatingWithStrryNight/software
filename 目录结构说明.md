# 项目目录结构详细说明

## 项目概述
本项目是一个基于 Android 的模块化应用项目，使用 Gradle 构建系统和 Kotlin 语言开发。项目名称为 `yike-app`，采用了模块化架构设计，便于代码组织和维护。

## 根目录结构

```
.
├── .git/                          # Git 版本控制目录
├── .gitignore                     # Git 忽略文件配置
├── .idea/                         # IntelliJ IDEA 项目配置目录
├── LICENSE                        # 开源许可证文件
├── README.md                      # 项目说明文档
├── build-logic/                   # 构建逻辑模块（自定义 Gradle 插件）
├── build.gradle.kts              # 根项目 Gradle 构建配置文件
├── buildSrc/                      # Gradle buildSrc 目录（构建脚本依赖）
├── gradle/                        # Gradle 包装器相关文件
├── gradle.properties             # Gradle 全局属性配置
├── gradlew                        # Gradle 包装器脚本（Unix/Linux/Mac）
├── gradlew.bat                    # Gradle 包装器脚本（Windows）
├── lib-res/                       # 公共资源库模块
├── libs.versions.toml            # Gradle 版本目录配置文件
├── local.properties              # 本地配置文件（包含 SDK 路径等）
├── module-base/                   # 基础模块
├── module-core/                   # 核心业务模块
├── module-datasource/            # 数据源模块
├── module-image-picker/          # 图片选择器模块
├── module-image-preview/         # 图片预览模块
├── module-main/                   # 主模块
├── module-qrcode/                # 二维码模块
├── module-user/                   # 用户模块
├── module-widget/                # 小部件模块
├── settings.gradle.kts           # Gradle 设置文件（模块配置）
├── 数据表关系.drawio.png         # 数据表关系图
└── 软件工程nabcd.html             # 软件工程 NABCD 分析文档
```

## 核心配置文件详解

### 1. 构建配置文件

#### build.gradle.kts（根项目构建文件）
- **作用**：定义项目级别的构建配置和插件
- **主要内容**：
  - Android Gradle Plugin (AGP) 版本配置
  - Kotlin 插件配置
  - KSP（Kotlin Symbol Processing）配置
  - Hilt 依赖注入插件
  - Compose Compiler 插件

#### settings.gradle.kts（项目设置文件）
- **作用**：定义项目结构和模块包含关系
- **主要内容**：
  - 插件管理配置
  - Maven 仓库配置（Google、Maven Central、阿里云镜像、JitPack）
  - 版本目录配置（libs.versions.toml）
  - 模块包含配置
  - 外部模块引用（opensource、product 目录）

#### libs.versions.toml（版本目录文件）
- **作用**：集中管理项目依赖版本
- **主要版本信息**：
  - AGP: 8.11.2
  - Kotlin: 2.2.10
  - Compose: 1.9.2
  - Android SDK: 编译版本 36，目标版本 35，最低版本 26
  - 第三方库：Retrofit、Room、Hilt、KComponent 等

#### gradle.properties
- **作用**：Gradle 全局属性配置
- **典型配置**：JVM 参数、Android 构建选项等

### 2. Gradle 包装器

#### gradle/wrapper/
- **gradle-wrapper.jar**：Gradle 包装器可执行 JAR
- **gradle-wrapper.properties**：Gradle 版本和下载配置

#### gradlew / gradlew.bat
- **作用**：跨平台 Gradle 包装器脚本，确保使用统一的 Gradle 版本

## 构建逻辑模块详解

### build-logic/
自定义构建逻辑的独立 Gradle 项目，用于定义可复用的 Gradle 插件。

```
build-logic/
├── convention/
│   ├── build.gradle.kts          # 构建逻辑模块的构建文件
│   └── src/
│       └── main/
│           └── kotlin/
│               ├── AppPlugin.kt              # 应用模块插件
│               ├── BasicConfigPlugin.kt      # 基础配置插件
│               ├── Commons.kt                # 公共配置和工具
│               ├── CompilerConfigPlugin.kt   # 编译器配置插件
│               ├── LibPlugin.kt              # 库模块插件
│               ├── ModuleBusinessPlugin.kt   # 业务模块插件
│               └── ModulePlugin.kt           # 通用模块插件
└── settings.gradle.kts            # 构建逻辑设置文件
```

#### 自定义插件说明：
1. **AppPlugin**: 用于应用级模块（app）的配置
2. **ModulePlugin**: 用于通用功能模块的配置（如 lib-res、module-base）
3. **ModuleBusinessPlugin**: 用于业务功能模块的配置（如各种业务模块）
4. **LibPlugin**: 用于纯库模块的配置
5. **BasicConfigPlugin**: 提供基础 Android 配置
6. **CompilerConfigPlugin**: 配置编译器选项
7. **Commons.kt**: 提供共享的配置常量和工具函数

### buildSrc/
Gradle 的特殊目录，用于构建脚本的依赖和配置。

## 功能模块详解

项目采用模块化架构，每个模块都是一个独立的 Android Library 模块。

### 模块通用结构
每个模块的标准目录结构如下：
```
module-xxx/
├── build.gradle.kts              # 模块构建配置
├── consumer-rules.pro            # ProGuard 消费者规则
├── proguard-rules.pro            # ProGuard 混淆规则
└── src/
    └── main/
        ├── AndroidManifest.xml   # Android 清单文件
        └── java/                 # 源代码目录
            └── com/
                └── xiaojinzi/
```

### 1. lib-res（公共资源库）
- **作用**：集中管理应用的公共资源
- **命名空间**：com.xiaojinzi.tally.lib.res
- **内容**：
  - **anim/**：动画资源（淡入淡出、滑动动画等）
  - **drawable/**：矢量图标资源（200+ 图标，涵盖金融、生活、购物等场景）
  - **drawable-[density]/**：不同密度的位图资源
  - **font/**：字体资源
  - **raw/**：JSON 动画文件（Lottie 动画）
  - **values/**：颜色、字符串、主题等资源
  - **values-night/**：夜间模式主题
  - **values-zh/**：中文字符串资源

### 2. module-base（基础模块）
- **作用**：提供应用的基础功能和依赖
- **命名空间**：com.xiaojinzi.tally.module.base
- **主要依赖**：
  - lib-res（资源库）
  - xiaojinzi-android-support 系列（初始化、Activity 栈、Retrofit 扩展等）
  - xiaojinzi-android-module 系列（存储、FFmpeg、阿里云 OSS、支付宝、微信 SDK）
  - xiaojinzi-android-reactive（响应式编程核心）
- **特点**：作为其他业务模块的基础依赖，提供通用功能

### 3. module-core（核心业务模块）
- **作用**：应用的核心业务逻辑模块
- **命名空间**：com.xiaojinzi.tally.module.core
- **插件**：使用 moduleBusinessPlugin
- **特点**：包含应用的核心业务实现

### 4. module-datasource（数据源模块）
- **作用**：处理网络数据源和 API 通信
- **命名空间**：com.xiaojinzi.tally.module.network
- **主要依赖**：
  - OkHttp3：HTTP 客户端
  - Retrofit：类型安全的 HTTP 客户端
  - Retrofit Gson Converter：JSON 转换器
- **特点**：封装网络请求和数据处理逻辑

### 5. module-image-picker（图片选择器模块）
- **作用**：提供图片选择和裁剪功能
- **命名空间**：com.xiaojinzi.tally.module.imagepicker
- **主要依赖**：
  - PictureSelector v3.11.2（图片选择器核心）
  - 图片压缩库
  - uCrop（图片裁剪）
  - CameraX（相机功能）
- **特点**：集成完整的图片选择、裁剪、压缩流程

### 6. module-image-preview（图片预览模块）
- **作用**：提供图片预览和浏览功能
- **命名空间**：com.xiaojinzi.tally.module.imagepreview
- **特点**：独立的图片预览功能模块

### 7. module-main（主模块）
- **作用**：应用主界面和导航功能
- **命名空间**：com.xiaojinzi.tally.module.main
- **插件**：使用 moduleBusinessPlugin
- **特点**：应用的主要入口和导航逻辑

### 8. module-qrcode（二维码模块）
- **作用**：提供二维码扫描和生成功能
- **命名空间**：com.xiaojinzi.tally.module.qrcode
- **主要依赖**：
  - Google ZXing Core 3.5.3（二维码核心库）
  - Quickie 1.9.0（快速二维码扫描）
- **特点**：完整的二维码功能支持

### 9. module-user（用户模块）
- **作用**：处理用户相关功能（登录、注册、个人信息等）
- **命名空间**：com.xiaojinzi.tally.module.user
- **插件**：使用 moduleBusinessPlugin
- **特点**：用户管理和认证功能

### 10. module-widget（小部件模块）
- **作用**：提供 Android 桌面小部件功能
- **命名空间**：com.xiaojinzi.tally.module.widget
- **主要依赖**：
  - Glance AppWidget（Jetpack Glance 小部件库）
  - Glance Material3（Material 3 设计支持）
- **特点**：
  - 使用 View Binding
  - 支持桌面小部件功能

## 外部模块引用

根据 `settings.gradle.kts` 的配置，项目还引用了外部目录的模块：

### opensource/ 目录（开源版本）
- **app**：开源版应用模块（:app:opensource）
- **module-network**：开源版网络模块（:module-network:opensource）

### product/ 目录（产品版本，可选）
如果存在 `../product` 目录，将包含：
- **app/domestic**：国内版应用（:app:domestic）
- **app/abroad**：国际版应用（:app:abroad）
- **module-network**：产品版网络模块（:module-network:product）
- **module-datasync**：产品版数据同步模块（:module-datasync:product）

## 技术栈总结

### 核心技术
- **语言**：Kotlin 2.2.10
- **构建工具**：Gradle 8.11.2
- **Android SDK**：最低 26，目标 35，编译 36

### 架构组件
- **UI 框架**：Jetpack Compose 1.9.2
- **依赖注入**：Hilt 2.57.2
- **组件化**：KComponent v2.2.1
- **响应式编程**：AndroidReactive v1.0.4

### 网络与数据
- **网络请求**：Retrofit 3.0.0 + OkHttp3
- **数据库**：Room 2.8.1
- **数据序列化**：Gson

### 其他功能
- **图片处理**：PictureSelector、uCrop、Glide
- **动画**：Lottie 6.6.9
- **后台任务**：WorkManager 2.10.5
- **二维码**：ZXing
- **支付**：支付宝、微信支付
- **云存储**：阿里云 OSS

## 模块依赖关系

```
┌─────────────────┐
│   app 层        │  ← :app:opensource / :app:domestic / :app:abroad
└────────┬────────┘
         │
    ┌────┴────────────────────────────────┐
    │                                     │
┌───▼──────────┐                  ┌──────▼──────┐
│ 业务模块层    │                  │  网络层      │
│ - module-main│                  │ - module-   │
│ - module-user│                  │   network   │
│ - module-core│                  │ - module-   │
│ - module-    │                  │   datasync  │
│   qrcode     │                  │             │
│ - module-    │                  │             │
│   image-*    │                  │             │
│ - module-    │                  │             │
│   widget     │                  │             │
└───┬──────────┘                  └──────┬──────┘
    │                                    │
    └────────────┬───────────────────────┘
                 │
        ┌────────▼────────┐
        │  基础设施层      │
        │ - module-base   │
        │ - module-       │
        │   datasource    │
        └────────┬────────┘
                 │
        ┌────────▼────────┐
        │  资源层          │
        │ - lib-res       │
        └─────────────────┘
```

## 构建和运行

### 环境要求
- JDK 17 或更高版本
- Android Studio Ladybug 或更高版本
- Android SDK 36

### 构建命令
```bash
# 清理构建
./gradlew clean

# 编译项目
./gradlew build

# 运行开源版本
./gradlew :app:opensource:installDebug

# 运行产品版本（如果存在）
./gradlew :app:domestic:installDebug
./gradlew :app:abroad:installDebug
```

## 项目特点

1. **模块化设计**：将功能拆分为独立模块，降低耦合度，提高可维护性
2. **多版本支持**：支持开源版和产品版（国内版/国际版）
3. **现代化技术栈**：使用最新的 Android 开发技术（Compose、Hilt、KSP 等）
4. **构建逻辑复用**：通过自定义 Gradle 插件统一模块配置
5. **版本集中管理**：使用 Version Catalog（libs.versions.toml）管理依赖版本
6. **国际化支持**：支持多语言（中文等）
7. **主题支持**：支持日间/夜间模式

## 开发团队

- **项目负责人**：刘子墨
- **贡献者**：
  - 何秉一
  - 刘子墨
  - 李子龙
  - 相子旋

## 许可证

本项目采用 LICENSE 文件中指定的开源许可证。

---

*文档生成时间：2025-12-05*
*项目版本：基于当前代码库快照*
